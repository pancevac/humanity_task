<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Humanity Shifts Task</title>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
</head>
<body>
<div class="col-md-8">
    <table class="table">
        <thead class="thead-dark">
        <tr>
            <th scope="col">Employee</th>
            <th scope="col">Position (Schedule)</th>
            <th scope="col">Shift</th>
            <th scope="col">Timeclock</th>
        </tr>
        </thead>
        <tbody>
        {% for shift in shifts %}
        <tr>
            <td>{{ shift.getEmployee.getName }}</td>
            <td>{{ shift.getPosition }}</td>
            <td>{{ shift.getStartDate }} - {{ shift.getEndDate }}</td>
            <td>{% if shift.getTimeClock %}
                {{ shift.getTimeClock.getInTime }} - {{ shift.getTimeClock.getOutTime }}
                {% else %}
                /
                {% endif %}
            </td>
        </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
</body>
</html>